{
  "name": "deserted-island-demo",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "First-person deserted island WebGPU demo for ThreeBrowser Runtime and GitHub Pages.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs",
    "pages:verify": "node scripts/verify-pages-build.mjs",
    "manifest": "node scripts/write-pull-manifest.mjs"
  },
  "dependencies": {
    "three": "0.184.0"
  },
  "devDependencies": {
    "vite": "8.2.2"
  }
}
